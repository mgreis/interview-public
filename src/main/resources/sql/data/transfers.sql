DROP TABLE IF EXISTS TRANSFER;

CREATE TABLE TRANSFER(ID BIGINT NOT NULL PRIMARY KEY,AMOUNT DOUBLE NOT NULL,SOURCE_ID BIGINT NOT NULL,TARGET_ID BIGINT NOT NULL,
TRANSFER_TIME TIMESTAMP AS CURRENT_TIMESTAMP,
FOREIGN KEY (SOURCE_ID) REFERENCES ACCOUNT(ID),FOREIGN KEY (TARGET_ID) REFERENCES ACCOUNT(ID));
